
name: test TO condition

on: 
  workflow_dispatch:
    inputs:
      name:
        description: 'my name'     
        required: false
      age:
        description: 'age'
        required: false

jobs:
  printInputs:
    runs-on: ubuntu-latest
    if: (${{ github.event.inputs.name}} != 0 && ${{ github.event.inputs.age }} != 0)
    steps:
    - run: |
        echo "Log level: ${{ github.event.inputs.name }}"
        echo "Tags: ${{ github.event.inputs.age }}" 
